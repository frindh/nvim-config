require("blink.cmp").setup({

    sources = {
        default = { "lsp", "path", "buffer", "snippets" },
    },

    keymap = {
        preset = "default",
    },

    --signature = { enabled = true },
})
